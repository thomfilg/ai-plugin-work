---
name: install-work-statusline
description: Register (or remove) the /work status bar — a live, agent-free ⚙ bar in Claude Code showing the current /work step, position (done/total), per-step detail (implement → which task, check → what it's doing), and a budget-coloured on-step timer from .work-state.json. Yields to the 🔄 follow-up bar during the follow_up step. Use when the user says "install the work statusline", "show work progress in the status bar", "register the work status bar", or "remove the work statusline".
argument-hint: "[--print | --remove]"
user_invocable: true
---

<!-- codex:plugin-root-preamble v1 — generated by scripts/codemod-plugin-root-preamble.js; edit the codemod, then re-run -->

> **Resolve the plugin root before running any script below.** A codex shell
> can inherit a stale, set-but-WRONG `CLAUDE_PLUGIN_ROOT` from its launcher,
> so a bare `${CLAUDE_PLUGIN_ROOT:-…}` fallback is NOT safe — validate the
> value against this skill's own SKILL.md first. Set `SKILL_MD` to this
> file's absolute path (codex lists it next to this skill in your skills
> list; under Claude Code the env var is always correct and the fallback
> branch never runs):
>
> ```bash
> SKILL_MD="/absolute/path/to/this/SKILL.md"   # from your skills list
> if [ -f "${CLAUDE_PLUGIN_ROOT:-/nonexistent}/skills/install-work-statusline/SKILL.md" ] &&
>    [ "${CLAUDE_PLUGIN_ROOT}/skills/install-work-statusline/SKILL.md" -ef "$SKILL_MD" ]; then
>   PLUGIN_ROOT="$CLAUDE_PLUGIN_ROOT"                         # validated — use as-is
> else
>   PLUGIN_ROOT="$(cd "$(dirname "$SKILL_MD")/../.." && pwd)" # stale/unset — self-locate
> fi
> ```
>
> Wherever this skill writes `${CLAUDE_PLUGIN_ROOT}`, substitute the validated
> `$PLUGIN_ROOT` value (shells do NOT share variables between tool calls —
> re-resolve or inline the resolved path in each command).

# /work status bar

Registers a Claude Code `statusLine` that renders the live `/work` state —
agent-free. It reads the artifacts the engine **already** writes — no new
files: the `.work.pid` marker (located via the plugin's own `findActiveMarker`,
scoped to this Claude session) and `.work-state.json` (`currentStep`,
`stepStatus`, `lastTransitionTimestamp`, `tasksMeta`, `checkProgress`) under
`<TASKS_BASE>/<ticket>/`.

Line format (one segment for the run owned by this session):

```
⚙ <ticket> · ▶ <step> (<done>/<total>) · <per-step detail> · <on-step timer>
```

- **`<step>`** — the step the run is on (`implement`, `check`, `pr`, …), with the
  overall position as completed/total.
- **Per-step detail** (its own tiny renderer per step, so new steps are a
  one-line registry entry — see `lib/step-detail.js`):
  - `implement` → `task <i>/<n>: <title>` (the task being built)
  - `check` → `retry N (check→implement)` or `running checks`
  - `task_review` → `fix round N` or `reviewing tasks`
- **On-step timer** — how long the run has sat on this step, coloured by the
  step's budget (the SAME per-phase budgets the maestro conductor nudges on):
  **green** ≤ budget · **yellow** ≤ 2× budget · **red** > 2× budget.

**Follow-up hand-off.** While the run is on the `follow_up` step the work line
goes empty so the 🔄 follow-up bar is the only line shown; when the run advances
to `ci` the work bar returns. The two are mutually exclusive by design, so
installing both is safe.

## Commands

- **Install:** `node "${CLAUDE_PLUGIN_ROOT}/scripts/workflows/work/statusline/install-work-statusline.js"`
- **Show resolved paths + current config:** `... install-work-statusline.js --print`
- **Remove (drop just the work bar):** `... install-work-statusline.js --remove`

After installing, run `/reload-plugins` (or wait for the next refresh tick).

Install any subset of **maestro → work → follow-up → qc** in any order — the
shared host renders whatever is registered, stacked by fragment name (10/20/30/40).

## How it fits together

- **Host:** `~/.claude/statusline-host.sh` — the single registered statusLine; renders
  every fragment under `~/.claude/statuslines/*.cmd` in filename order.
- **Fragment:** `~/.claude/statuslines/20-work.cmd` — one line, the path to this
  plugin's renderer. Owned solely by the work installer.
- **Renderer:** `scripts/workflows/work/statusline/work-statusline.sh` → `work-statusline.js`.
- **Data source:** the engine's existing `<TASKS_BASE>/<ticket>/.work-state.json` + `.work.pid` (no files created by the bar).

## Codex

The `/stats` and `/health` skills work on codex (plain node, no symlink deps).
The **bar itself cannot render on codex CLI**: codex does have a status line
(`/statusline` → `tui.status_line` in `~/.codex/config.toml`), but it renders
only a fixed enum of built-in fields (model, git branch, token counters, cwd,
version…) — it does **not** support command-backed renderers like this one yet
([openai/codex#20140](https://github.com/openai/codex/issues/20140)). Under
`AGENT_RUNTIME=codex` the installer therefore refuses cleanly (exit 0) and
prints the CLI alternative. The codex IDE extension is a sidebar, not a bottom
status bar, so it has no equivalent surface either.

| Claude Code | Codex CLI |
|---|---|
| this ⚙ `statusLine` bar | `/statusline` (built-in fields only — can't show `/work` state) |
| status/details | `/status` |
| terminal title | `/title` |
| background command status | `/ps` |

For live `/work` state on codex, watch the state file directly:
`watch -n 3 'cat <TASKS_BASE>/<ticket>/.work-state.json'`.
